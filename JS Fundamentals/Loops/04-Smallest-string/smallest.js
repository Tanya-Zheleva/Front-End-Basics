'use strict';

function solve() {
    // let documentMin = '';

    // for (let key in document) {
    //    console.log(document[key]);
       
    // }
    let el = document.getElementById('test');
    console.log(el);
}

solve();